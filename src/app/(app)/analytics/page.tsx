import { PhasePlaceholder } from "@/components/ui/phase-placeholder";

export const metadata = { title: "Analytics — Signal" };

export default function AnalyticsPage() {
  return (
    <PhasePlaceholder
      title="Analytics"
      subtitle="Facebook + Instagram"
      phase="Phase 3"
      description="Reach and engagement charts, intent-by-format, follower split and the scored post table come online in Phase 3 with the sync engine."
    />
  );
}

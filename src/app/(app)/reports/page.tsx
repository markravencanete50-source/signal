import { PhasePlaceholder } from "@/components/ui/phase-placeholder";

export const metadata = { title: "Reports — Signal" };

export default function ReportsPage() {
  return (
    <PhasePlaceholder
      title="Reports"
      subtitle="White-label · auto-refreshing public links"
      phase="Phase 5"
      description="The report builder, Claude narrative, white-label public pages and scheduled digests arrive in Phase 5."
    />
  );
}
